server:
  address: 127.0.0.1
  port: 8091

spring:
  application:
    name: execution-worker

ops-agent:
  worker:
    transport-auth:
      enabled: false
      max-clock-skew: 30s
    sql-egress:
      allowed-targets: []
      connections: []
